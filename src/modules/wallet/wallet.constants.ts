/** Merchant wallet product vertical (`vendorCode`). */
export const WALLET_VENDOR_CODE = 'sportsbook';

/** Merchant `gameType`: 3 = Other (sports betting). */
export const WALLET_GAME_TYPE = 3;

/** Fallback when league snapshot is missing on older legs. */
export const WALLET_UNKNOWN_GAME_CODE = 'sportsbook_unknown';
